image: node:4.4.4

pages:
  cache:
    paths:
    - node_modules/
    - bower_components/

  script:
    - npm install --silent
    - ./node_modules/bower/bin/bower -s install
    - grunt dist
  artifacts:
    paths:
    - public